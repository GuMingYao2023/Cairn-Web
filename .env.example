# ── Cairn API Server Authentication ─────────────────────────────────────
# Set these to enable HTTP Basic Auth on the web UI (port 8000).
# Leave both unset (or empty) to run without authentication for local dev.
#
#   cp .env.example .env   # then edit with your credentials

CAIRN_USER=
CAIRN_PASS=
